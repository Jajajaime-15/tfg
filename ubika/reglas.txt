{
  "rules": {
    "usuarios": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth != null"
      }
    },
    "grupos": {
      ".read": "auth != null",
      ".write": "auth != null",
      "$grupo_id": {
        ".read": "auth != null && root.child('grupos').child($grupo_id).child('miembros').child(auth.uid).exists()",
        ".write": "auth != null && (!data.exists() || data.child('admin').val() === auth.uid)"
      }
    },
    "ubicaciones": {
      ".read": "auth != null",
      ".write": "auth != null"
    }
  }
}